import React from "react";

function Button() {
  return (
    <div>
      <button
        type="button"
        className=""
        // onClick={(e) => setConvert(value)}
      >
        Convert
      </button>
    </div>
  );
}

export default Button;
